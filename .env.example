# AI Gateway Configuration
# Copy this file to .env and configure your settings

# Your AI Gateway ID (created in Cloudflare dashboard)
# This enables caching, analytics, and rate limiting for AI requests
AI_GATEWAY_ID=your-gateway-id-here

# Optional: If you want to override the gateway ID for local development
# AI_GATEWAY_ID_DEV=dev-gateway

# Note: These are regular environment variables, not secrets
# The gateway ID is not sensitive information